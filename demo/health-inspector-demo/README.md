# health-inspector demo fixture

Deliberately messy sample project used by `.github/workflows/self-test.yml` to prove the
Health Inspector action finds real violations end-to-end. Do not "fix" these on purpose —
that would break the self-test.
